diff --git a/spoolman/api/v1/models.py b/spoolman/api/v1/models.py
index a1b2c3d..e4f5g6h 100644
--- a/spoolman/api/v1/models.py
+++ b/spoolman/api/v1/models.py
@@ -433,6 +433,26 @@ class Plate(BaseModel):
         )
 
 
+class Printer(BaseModel):
+    id: int = Field(description="Unique internal ID of this printer.")
+    registered: SpoolmanDateTime = Field(description="When the printer was registered in the database. UTC Timezone.")
+    name: str = Field(min_length=1, max_length=256, description="Printer name.", examples=["Prusa i3 MK3S"])
+    model: str | None = Field(None, max_length=256, description="Optional printer model.", examples=["MK3S"])
+    location: str | None = Field(None, max_length=256, description="Optional printer location.", examples=["Lab 1"])
+    comment: str | None = Field(None, max_length=1024, description="Optional comment.")
+
+    @staticmethod
+    def from_db(item: models.Printer) -> "Printer":
+        return Printer(
+            id=item.id,
+            registered=item.registered,
+            name=item.name,
+            model=item.model,
+            location=item.location,
+            comment=item.comment,
+        )
+
+
 class PrintJobSpool(BaseModel):
     spool_id: int = Field(description="Spool ID used.")
     weight_used: float = Field(ge=0, description="Weight used from this spool in grams.")
@@ -451,6 +471,7 @@ class PrintJob(BaseModel):
     status: str = Field(max_length=64, description="Status of the print job (e.g. successful, pending).")
     start_time: SpoolmanDateTime | None = Field(None, description="Start time. UTC Timezone.")
     end_time: SpoolmanDateTime | None = Field(None, description="End time. UTC Timezone.")
+    printer_id: int | None = Field(None, description="Associated printer ID.")
     printer_name: str | None = Field(None, max_length=256, description="Printer name.")
     comment: str | None = Field(None, max_length=1024, description="Optional comment.")
     spool_usages: list[PrintJobSpool] = Field(default=[], description="Filament usage per spool.")
@@ -464,6 +485,7 @@ class PrintJob(BaseModel):
             status=item.status,
             start_time=item.start_time,
             end_time=item.end_time,
+            printer_id=item.printer_id,
             printer_name=item.printer_name,
             comment=item.comment,
             spool_usages=[PrintJobSpool.from_db(u) for u in item.spool_usages] if item.spool_usages else [],
@@ -535,3 +557,10 @@ class PrintJobEvent(Event):
 
     payload: PrintJob = Field(description="Updated print job.")
     resource: Literal["print_job"] = Field(description="Resource type.")
+
+
+class PrinterEvent(Event):
+    """Event."""
+
+    payload: Printer = Field(description="Updated printer.")
+    resource: Literal["printer"] = Field(description="Resource type.")
diff --git a/spoolman/api/v1/router.py b/spoolman/api/v1/router.py
index b2c3d4e..f5g6h7i 100644
--- a/spoolman/api/v1/router.py
+++ b/spoolman/api/v1/router.py
@@ -15,7 +15,7 @@ from spoolman.exceptions import ItemNotFoundError
 from spoolman.ws import websocket_manager
 
-from . import export, externaldb, field, filament, models, other, setting, spool, vendor, project, plate, print_job
+from . import export, externaldb, field, filament, models, other, setting, spool, vendor, project, plate, print_job, printer
 
 logger = logging.getLogger(__name__)
@@ -115,3 +115,4 @@ app.include_router(project.router)
 app.include_router(plate.router)
 app.include_router(print_job.router)
+app.include_router(printer.router)
diff --git a/spoolman/database/print_job.py b/spoolman/database/print_job.py
index c3d4e5f..g6h7i8j 100644
--- a/spoolman/database/print_job.py
+++ b/spoolman/database/print_job.py
@@ -14,7 +14,7 @@ from sqlalchemy.orm import joinedload
 from sqlalchemy.ext.asyncio import AsyncSession
 
-from spoolman.api.v1.models import EventType, PrintJob, PrintJobEvent
-from spoolman.database import models, plate, spool
+from spoolman.api.v1.models import EventType, PrintJob, PrintJobEvent
+from spoolman.database import models, plate, spool, printer
 from spoolman.database.utils import SortOrder, add_where_clause_str, add_where_clause_int
 from spoolman.exceptions import ItemNotFoundError, ItemDeleteError
@@ -32,7 +32,7 @@ async def create(
     end_time: datetime | None = None,
-    printer_name: str | None = None,
+    printer_id: int | None = None,
     comment: str | None = None,
     spool_usages: list[dict] | None = None,
 ) -> models.PrintJob:
@@ -54,12 +54,4 @@ async def create(
-    db_printer = None
-    if printer_name:
-        stmt = select(models.Printer).where(models.Printer.name == printer_name)
-        db_printer = (await db.execute(stmt)).scalars().first()
-        if db_printer is None:
-            db_printer = models.Printer(name=printer_name)
-            db.add(db_printer)
-            await db.flush()
+    if printer_id is not None:
+        await printer.get_by_id(db, printer_id)
 
     # Create the PrintJob
     print_job = models.PrintJob(
         plate_id=plate_id,
         status=status,
         registered=datetime.utcnow().replace(microsecond=0),
         start_time=start_time,
         end_time=end_time,
-        printer=db_printer,
+        printer_id=printer_id,
         comment=comment,
     )
@@ -120,6 +120,7 @@ async def find(
     plate_id: int | None = None,
     status: str | None = None,
+    printer_id: int | None = None,
     sort_by: dict[str, SortOrder] | None = None,
     limit: int | None = None,
     offset: int = 0,
 ) -> tuple[list[models.PrintJob], int]:
@@ -128,4 +129,5 @@ async def find(
     stmt = add_where_clause_int(stmt, models.PrintJob.plate_id, plate_id)
     stmt = add_where_clause_str(stmt, models.PrintJob.status, status)
+    stmt = add_where_clause_int(stmt, models.PrintJob.printer_id, printer_id)
 
     total_count = None
@@ -187,13 +189,5 @@ async def update(
-    if "printer_name" in data:
-        printer_name_val = data.pop("printer_name")
-        if printer_name_val is None or printer_name_val == "":
-            print_job.printer = None
-        else:
-            stmt = select(models.Printer).where(models.Printer.name == printer_name_val)
-            db_printer = (await db.execute(stmt)).scalars().first()
-            if db_printer is None:
-                db_printer = models.Printer(name=printer_name_val)
-                db.add(db_printer)
-                await db.flush()
-            print_job.printer = db_printer
+    if "printer_id" in data:
+        printer_id_val = data["printer_id"]
+        if printer_id_val is not None:
+            await printer.get_by_id(db, printer_id_val)
diff --git a/spoolman/api/v1/print_job.py b/spoolman/api/v1/print_job.py
index d4e5f6g..h7i8j9k 100644
--- a/spoolman/api/v1/print_job.py
+++ b/spoolman/api/v1/print_job.py
@@ -38,3 +38,3 @@ class PrintJobParameters(BaseModel):
     end_time: datetime | None = Field(None, description="End time. UTC Timezone.")
-    printer_name: str | None = Field(None, max_length=256, description="Printer name.")
+    printer_id: int | None = Field(None, description="Printer ID.")
     comment: str | None = Field(None, max_length=1024, description="Optional comment.")
@@ -55,3 +55,3 @@ class PrintJobUpdateParameters(BaseModel):
     end_time: datetime | None = Field(None, description="End time. UTC Timezone.")
-    printer_name: str | None = Field(None, max_length=256, description="Printer name.")
+    printer_id: int | None = Field(None, description="Printer ID.")
     comment: str | None = Field(None, max_length=1024, description="Optional comment.")
@@ -97,2 +97,9 @@ async def find(
     status: Annotated[
         str | None,
         Query(
             title="Status",
             description="Filter by print job status.",
         ),
     ] = None,
+    printer_id: Annotated[
+        int | None,
+        Query(
+            title="Printer ID",
+            description="Filter by printer ID.",
+        ),
+    ] = None,
     sort: Annotated[
@@ -124,3 +131,4 @@ async def find(
             plate_id=plate_id,
             status=status,
+            printer_id=printer_id,
             sort_by=sort_by,
@@ -215,3 +223,3 @@ async def create(
             end_time=body.end_time,
-            printer_name=body.printer_name,
+            printer_id=body.printer_id,
             comment=body.comment,
diff --git a/tests_integration/tests/conftest.py b/tests_integration/tests/conftest.py
index e5f6g7h..i8j9k0l 100644
--- a/tests_integration/tests/conftest.py
+++ b/tests_integration/tests/conftest.py
@@ -235,4 +235,21 @@ def random_project_impl():
 
+@contextmanager
+def random_printer_impl():
+    """Return a random printer."""
+    result = httpx.post(
+        f"{URL}/api/v1/printer",
+        json={
+            "name": "Integration Test Printer",
+            "model": "Prusa i3 MK3S",
+            "location": "Lab 1",
+            "comment": "Testing printer CRUD",
+        },
+    )
+    result.raise_for_status()
+    printer: dict[str, Any] = result.json()
+    yield printer
+    httpx.delete(f"{URL}/api/v1/printer/{printer['id']}").raise_for_status()
+
+
 @contextmanager
 def random_plate_impl(project_id: int):
@@ -277,3 +294,3 @@ def random_plate_impl(project_id: int):
-def random_print_job_impl(plate_id: int):
+def random_print_job_impl(plate_id: int, printer_id: int | None = None):
     """Return a random print job."""
-    result = httpx.post(
-        f"{URL}/api/v1/print_job",
-        json={
-            "plate_id": plate_id,
-            "status": "pending",
-            "printer_name": "Ender 3",
-            "comment": "Initial test job",
-            "spool_usages": [],
-        },
-    )
+    payload = {
+        "plate_id": plate_id,
+        "status": "pending",
+        "comment": "Initial test job",
+        "spool_usages": [],
+    }
+    if printer_id is not None:
+        payload["printer_id"] = printer_id
+
+    result = httpx.post(
+        f"{URL}/api/v1/print_job",
+        json=payload,
+    )
     result.raise_for_status()
@@ -298,2 +315,9 @@ def random_project():
 
+@pytest.fixture
+def random_printer():
+    """Return a random printer."""
+    with random_printer_impl() as printer:
+        yield printer
+
+
 @pytest.fixture
@@ -312,3 +336,3 @@ def random_plate(random_project):
-def random_print_job(random_plate):
+def random_print_job(random_plate, random_printer):
     """Return a random print job."""
-    with random_print_job_impl(random_plate["id"]) as print_job:
+    with random_print_job_impl(random_plate["id"], random_printer["id"]) as print_job:
         yield print_job
diff --git a/tests_integration/tests/print_job/test_crud.py b/tests_integration/tests/print_job/test_crud.py
index f6g7h8i..j9k0l1m 100644
--- a/tests_integration/tests/print_job/test_crud.py
+++ b/tests_integration/tests/print_job/test_crud.py
@@ -9,12 +9,12 @@ from ..conftest import URL
-def test_create_print_job(random_plate: dict[str, Any]):
+def test_create_print_job(random_plate: dict[str, Any], random_printer: dict[str, Any]):
     """Test creating a print job."""
     payload = {
         "plate_id": random_plate["id"],
         "status": "printing",
-        "printer_name": "Ender 3",
+        "printer_id": random_printer["id"],
         "comment": "Initial test job",
         "spool_usages": [],
     }
     result = httpx.post(f"{URL}/api/v1/print_job", json=payload)
     assert result.status_code in (200, 201)
     print_job = result.json()
 
     assert "id" in print_job
     assert "registered" in print_job
     assert print_job["plate_id"] == random_plate["id"]
     assert print_job["status"] == payload["status"]
-    assert print_job["printer_name"] == payload["printer_name"]
+    assert print_job["printer_id"] == random_printer["id"]
+    assert print_job["printer_name"] == random_printer["name"]
     assert print_job["comment"] == payload["comment"]
     assert print_job["spool_usages"] == payload["spool_usages"]
 
@@ -34,3 +34,3 @@ def test_get_print_job(random_print_job: dict[str, Any]):
     assert print_job["id"] == random_print_job["id"]
-    assert print_job["printer_name"] == random_print_job["printer_name"]
+    assert print_job["printer_id"] == random_print_job["printer_id"]
+    assert print_job["printer_name"] == random_print_job["printer_name"]
 
@@ -54,3 +54,3 @@ def test_patch_print_job(random_print_job: dict[str, Any]):
-    new_printer = "Prusa i3"
-    result = httpx.patch(f"{URL}/api/v1/print_job/{random_print_job['id']}", json={"printer_name": new_printer})
+    result = httpx.patch(
+        f"{URL}/api/v1/print_job/{random_print_job['id']}",
+        json={"printer_id": random_printer["id"]},
+    )
     assert result.status_code == 200
     print_job = result.json()
     assert print_job["id"] == random_print_job["id"]
-    assert print_job["printer_name"] == new_printer
+    assert print_job["printer_id"] == random_printer["id"]
+    assert print_job["printer_name"] == random_printer["name"]
